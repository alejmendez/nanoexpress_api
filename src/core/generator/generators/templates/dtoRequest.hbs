class {{model}}Request {
  {{#each fields}}
  {{name}}: {{type}};
  {{/each}}

  constructor(data: any) {
    {{#each fields}}
    this.{{name}} = data.{{name}};
    {{/each}}
  }
}

export default {{model}}Request;
